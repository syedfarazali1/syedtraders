@model IEnumerable<POSApplication.Models.Ram>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Deshboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RamName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RamName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RamID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RamID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RamID })
        </td>
    </tr>
}

</table>
