@model IEnumerable<POSApplication.Models.HardDisk>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Deshboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HardDiskName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HardDiskName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HardDiskID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HardDiskID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HardDiskID })
        </td>
    </tr>
}

</table>
