@model IEnumerable<POSApplication.Models.Processor>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Deshboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProcessorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProcessorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProcessorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProcessorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProcessorID })
        </td>
    </tr>
}

</table>
